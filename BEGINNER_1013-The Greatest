#include<stdio.h>
#include<math.h>

int main()
{
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    int s = ((a+b)+abs(a-b))/2;
    int j = ((c+s)+abs(c-s))/2;
    printf ("%d eh o maior\n",j);
    return 0;
}
