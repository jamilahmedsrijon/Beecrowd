#include<stdio.h>
int main(){
    double A, B;
    scanf("%lf%lf", &A, &B);
    A = A * 3.5;
    B = B * 7.5;
    double avg = (A + B) / (3.5 + 7.5);
    printf("MEDIA = %.5lf\n", avg);
    return 0;
}
